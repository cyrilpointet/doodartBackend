{
  "routes": [
    {
      "method": "GET",
      "path": "/band-events",
      "handler": "band-event.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/band-events/count",
      "handler": "band-event.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/band-events/:id",
      "handler": "band-event.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/band-events",
      "handler": "band-event.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/band-events/:id",
      "handler": "band-event.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/band-events/:id",
      "handler": "band-event.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
